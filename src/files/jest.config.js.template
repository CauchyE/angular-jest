require('jest-preset-angular/ngcc-jest-processor');

module.exports = {
  preset: 'jest-preset-angular',
  setupFilesAfterEnv: ['<rootDir>/<%= projectRoot ? `${projectRoot}/` : '' %><%= projectName ? `${projectName}/` : '' %>src/setup-jest.ts'],
  testPathIgnorePatterns: ['<rootDir>/<%= projectRoot ? `${projectRoot}/` : '' %><%= projectName ? `${projectName}/` : '' %>e2e/']
};
